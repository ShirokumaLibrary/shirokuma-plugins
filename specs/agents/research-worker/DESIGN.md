# research-worker 設計メモ

エージェント管理用メタデータ。実行時には読み込まれない。

## コンセプト

公式ドキュメントとプロジェクトパターンを調査するサブエージェント。新機能の開始時やベストプラクティスが不明な場合に使用する。

## 設計判断

- Web アクセス必須（公式ドキュメント参照が主目的）
- Bash あり（プロジェクト構造の調査用）
- 書き込みツールなし — 調査結果はテキストで返し、永続化は呼び出し元が担当

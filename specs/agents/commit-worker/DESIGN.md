# commit-worker 設計メモ

エージェント管理用メタデータ。実行時には読み込まれない。

## コンセプト

ステージ→コミット→プッシュを担うサブエージェント。`working-on-issue` のチェーンの一部として `coding-worker` 完了後に起動される。

## 設計判断

- ツールを Bash, Read, Grep, Glob に限定し、誤ってファイルを編集するリスクを排除
- モデルは Sonnet（コスト効率。コミット操作に高度な推論は不要）
